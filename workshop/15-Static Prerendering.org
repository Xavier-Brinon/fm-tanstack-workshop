#+title: Static Pre-rendering
#+startup: indent
#+options: ^:{}

So you want to static pre-generate your app.

Two things to know:

* Pre-rendering routes
#+begin_src javascript
export default defineConfig({
  plugins: [
    tanstackStart({
      prerender: {
        // Enable prerendering
        enabled: true,
      },
    }),
  ],
});
#+end_src

[[https://x.com/leerob/status/1969256190241640946][leerob on X]]

See the docs for full options

[[https://tanstack.com/start/latest/docs/framework/react/guide/static-prerendering][static prerendering docs]]

* Static server function calls
It's a middleware you can install, and use

[[https://tanstack.com/start/latest/docs/framework/react/guide/static-server-functions][static server functions docs]]

Let's see a demo!

[[https://github.com/arackaf/tanstack-blog-blog-post][tanstack-blog-blog-post repo]]
